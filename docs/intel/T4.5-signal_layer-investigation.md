# T4.5: intel.signal_layer Investigation

**Date:** 2026-04-19  
**Finding:** Implementation complete; disabled pending X2 (Entity-Linker)

## Root Cause

`policy.intel.signal_layer.enabled: false` because:

1. `trading_cycle.py:2085-2144` already has complete wiring — the code is production-ready.
2. However, it requires `ctx.intel_sector_impacts`, `ctx.intel_supply_vulnerability`, `ctx.intel_sanctions_beneficiary`, `ctx.intel_chokepoint_exposure` to be populated.
3. These ctx fields require an **Entity→Ticker Linker (X2)** to map news entities (countries, companies, sectors) to specific ETF/equity symbols.
4. Without X2, `any(x is not None for x in [...])` is always `False` → the inner scoring block never runs.

## Activation Path

1. Implement X2 Entity-Linker (see `docs/intel/X2-entity-linker-spec.md` when written)
2. Wire X2 output into TradingContext (probably in `run_intel_cycle.py` or `trading_cycle.py` preamble)
3. Flip `policy.intel.signal_layer.enabled: true`
4. Run Shadow-Mode: observe `[INTEL] signal layer applied` logs for ≥7 days
5. User review → production flip

## No Code Change Required (2026-04-19)

The wiring in `trading_cycle.py` is correct. No bug. No refactor needed.  
This task is **PARK** until X2 is implemented.
