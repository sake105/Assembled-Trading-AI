# Paket 6 — GO_LIVE B3 + C3

**Datum:** 2026-05-28  
**Branch:** main

---

## B3 — Aktive Pilot-Strategie verbindlich konfiguriert

**Ziel:** Ein einziger, offizieller Konfigurationseintrag legt den aktiven Pilot-Strategienamen verbindlich fest.

**Änderungen:**

- `configs/policy.yaml` — neuer Abschnitt `paper_pilot.active_strategy: trend_baseline`
- `src/assembled_core/ops/paper_runner.py` — neue Funktion `_resolve_active_strategy(paper_cfg, policy)`:
  - liest `policy["paper_pilot"]["active_strategy"]`
  - überschreibt `app_cfg["paper_runner"]["strategy"]["name"]` wenn Key gesetzt und ≠ "none"
  - Fallback: bisheriges Verhalten (app_cfg), keine Breaking Change
- Wiring in `run_paper_daily_one()` (war Zeile 1104): lazy `load_policy()` + `_resolve_active_strategy()`

**Aktive Strategie:** `trend_baseline` — Pilot seit 2026-05-23, OOS CAGR +43% / Sharpe 1.44 (2025-01–2026-05).

---

## C3 — Kostensatz ehrlich festgeschrieben

**Ziel:** Paper-Engine liest 10 bps aus policy.yaml statt 0 bps (Simulate-Fills-Default) oder 1 bps (Legacy-Default aus costs.py).

**Änderungen:**

- `configs/policy.yaml` — `paper_pilot.cost_model: {commission_bps: 10.0, spread_w: 0.25, impact_w: 0.5}`
  - Kommentar: ANNAHME, nicht gegen echte Fills kalibriert. Quelle: OOS-Läufe 2026-05.
  - TODO Phase 2: kalibrieren sobald Live-Daten existieren.
- `src/assembled_core/ops/paper_runner.py` — neue Funktion `_resolve_cost_cfg(app_cfg, policy)`:
  - liest `policy["paper_pilot"]["cost_model"]`
  - überschreibt `app_cfg["paper_runner"]["cost_model"]` wenn Key gesetzt
  - Fallback: app_cfg, dann `{}` (simulate_fills setzt dann 0 bps — bestehend)
- Wiring in `_prd_paper_fills_and_ledger()` (war Zeile 550): lazy `load_policy()` + `_resolve_cost_cfg()`

**Kostensatz:** 10 bps commission — konsistent mit OOS-Läufen in `scripts/_oos_walk_forward_trend_baseline.py`.

---

## Tests

`tests/test_paper_runner_paket6.py` — 16 Tests, 4 Klassen:

- `TestResolveActiveStrategy` (6 Tests): policy override, fallback zu app_cfg, fallback zu "none", "none"-String blockiert Override, leerer String blockiert Override, fehlender paper_pilot-Key
- `TestResolveCostCfg` (6 Tests): policy override mit allen 3 Feldern, fallback zu app_cfg, leeres Dict bei fehlendem beidseitigem Key, 10 bps ≠ 1 bps Legacy-Default, fehlender paper_pilot-Key, defensive-copy-Test
- `TestLoadPilotPolicyFailFast` (4 Tests): ValueError re-raised, yaml.YAMLError re-raised, FileNotFoundError → leeres Dict (Soft-Fallback), Happy-Path gibt Policy-Dict zurück

---

## Testergebnis

Lokal: 16/16 PASS (`pytest tests/test_paper_runner_paket6.py -m fast -v`).  
CI: unverified.
